version: '3.8'

services:
  example-mcp-stdio:
    build:
      context: .
      dockerfile: Dockerfile
    image: mcp/example-mcp-stdio:latest
    stdin_open: true
    tty: true
    container_name: example_mcp_stdio
    restart: unless-stopped
    environment:
      - NODE_ENV=production
