id: yigitkonur/example-mcp-stdio
name: Example MCP STDIO
description: MCP TypeScript SDK v2 (pre-release) stdio starter with built-in scaffolding CLI
repository: https://github.com/yigitkonur/example-mcp-stdio
license: MIT

startCommand:
  type: stdio
  command: node
  args: ['dist/index.js', 'serve']
  cwd: ./

configSchema:
  type: object
  properties:
    LOG_LEVEL:
      type: string
      enum: ['debug', 'info', 'warn', 'error']
      default: 'info'
      description: Logging level used by your MCP host/client
  required: []
